# Frame 中文增强版（zh-cn）更新总账与封版归档

- **版本标签**：`v0.33.1-zh-cn`
- **上游基准**：`eefde7a4` (Frame v0.33.1)
- **分支状态**：`zh-cn`（通过全量 803 项单元测试，编译 0 error / 0 warning）

---

## 一、 界面与交互体验

### 1. 全界面中文本地化
- 全面覆盖菜单、按钮、提示信息（Tooltip）、系统通知与错误提示；
- 术语标准化：统一采用专业行业译名（如 CRF「恒定速率因子」、ABR「平均码率」、VBR「动态码率」等）。

### 2. 顶栏预设菜单（Preset Menu）
- 从设置面板深层移至窗口顶栏，操作路径大幅缩短；
- 支持自定义预设的快速创建、切换、重命名、删除与恢复默认预设；
- 预设切换与当前勾选文件智能联动，具备未保存草稿保护。

### 3. 设置面板重组
- **视频页**：下半区重组为固定四节（编码器与率控、尺寸与帧率、高级编码参数、硬件加速），视觉层级分明；
- **音频页**：重构为四行紧凑下拉化布局，码率单位统一规范为 `kbps`，质量等级汉化；
- **源信息页**：新增“码率分析”视图，支持自适应动态网格、均值线与峰值线可视化绘制。

### 4. Windows 全程静默无控制台黑窗
- 主程序声明 `#![windows_subsystem = "windows"]`；
- 所有外部子进程（`ffmpeg`、`ffprobe` 等）调用统一封装 `tool_command`，配置 `CREATE_NO_WINDOW`（`0x0800_0000`）；
- 从软件启动、源文件探测、实时预览到批量转码，全程彻底告别控制台黑窗闪烁。

---

## 二、 预览与播放引擎优化

### 1. 就近关键帧内存缓存（`PreviewFrameCache`）
- 引入 24 帧固定容量的 LRU 内存缓存队列，常驻内存仅约 60~80MB；
- 在用户来回拉动滑条或在剪辑点反复微调时，命中缓存直接跳过外部 `ffmpeg.exe` 进程拉起，延迟降至 **0ms 纯内存级响应**；
- 严谨的帧周期容差匹配（`0.45 / fps`）：微动或同一物理帧内绝不重复解码；
- 严格区分快速寻道（`precise = false`）与精准停靠（`precise = true`），确保松手确认的剪辑点分毫不差。

### 2. 滑条双管线统一与空间硬截断
- **消除按下死锁**：滑条按下瞬间（MouseDown）改为快速寻道（`SeekFast`），彻底消除 150~300ms 解码互斥锁卡死；
- **管线彻底统一**：播放头竖线（Scrub）与两端白条（Trim Handles）完全统一到 16ms 节流快速流水线中，手感如丝顺滑；
- **空间硬截断机制（方案 A）**：鼠标拖拽超出滑条红框边界瞬间立即中止控制并精准恢复原画面，在物理松手前移回亦绝不继续控制；配合全局抬起捕获，会话生命周期严格闭环。

### 3. 拖窗定格跳帧与 5 秒暖暂停
- 拖动窗口期间 GPUI 级跳过整帧重绘，避免 Windows 消息循环拥塞导致的卡顿与白屏；
- 引入基于纪元计数器（`warm_pause_epoch`）的 5 秒暖暂停：暂停 5 秒内保持子进程就绪，再次播放 0ms 瞬间续播；超时后安全释放资源。

---

## 三、 转码核心与 FFmpeg 参数（frame-core）

### 1. 硬件解码解耦（`hwaccel.rs`）
- 修复了原版官方版本在勾选“硬件解码”后，因无条件追加 CPU 侧 `pad` 滤镜，导致 NVENC 转换 100% 以 `-22 (Invalid argument)` 崩溃产出 0 字节的重大缺陷；
- 判定显存直通路径（`vram_path_available`）：仅在流水线无任何 CPU 滤镜且源为偶数尺寸时直通显存；其余情况自动剥除 `-hwaccel_output_format`，平滑回落至内存下载。

### 2. 软件两遍编码系统（`twopass.rs`）
- 支持 `libx264`、`libx265`、`libsvtav1` 两遍码率控制；
- 第一遍采用 `-an -f null -`，跳过音频解码并丢弃视频输出，使第一遍速度提升数倍；
- 按任务 ID 生成独立统计文件（`frame-two-pass-{task_id}`），杜绝并发任务踩踏，任务完成自动清理临时日志。

### 3. 心理视觉优化与编码器参数门控（`psy.rs` & `profile.rs`）
- 心理视觉优化参数严格按编码器门控（x264 发 `psy`/`psy-rd`，x265 发 `psy-rd`/`psy-rdoq`），超出范围数值自动过滤，防止命令行注入；
- 兼容性 Profile 与像素格式联动：x264 8-bit Profile 与 10-bit 像素格式互斥拦截；ProRes 4444/4444XQ 强制联动切至 `yuv444p10le`，避免产出坏流。

### 4. 现代容器规范（`container.rs` & `args.rs`）
- HEVC 进 MP4/MOV 时自动补 `-tag:v hvc1`，解决 Apple 体系（QuickTime、Safari、iOS）无法解码的问题；
- MP4/MOV 容器自动追加 `-movflags +faststart`，将 `moov` 索引前置，极大提升网络串流与本地播放器的首帧起播速度。

---

## 四、 上游同步 runbook（作者更新后操作指南）

未来当上游作者（66HEX/frame）发布新版本后，直接运行内置同步脚本：

```bash
./scripts/sync-upstream.sh
```

该脚本将自动执行：
1. `git fetch upstream` 获取上游最新提交；
2. 预判上游改动范围并高亮我方注入文件的变动；
3. 执行 `git rebase --onto upstream/master eefde7a zh-cn`，按原子提交栈逐个重放；
4. 若有局部冲突，解决后 `git add <file> && git rebase --continue`；
5. 执行 `git range-diff` 严格对账；
6. 运行全量基线测试验证。
